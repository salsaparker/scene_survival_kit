<%= form_for(@merch_artist) do |f| %>
  <% if @merch_artist.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@merch_artist.errors.count, "error") %> prohibited this merch_artist from being saved:</h2>

      <ul>
      <% @merch_artist.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
