<%= form_for(@visual_artist) do |f| %>
  <% if @visual_artist.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@visual_artist.errors.count, "error") %> prohibited this visual_artist from being saved:</h2>

      <ul>
      <% @visual_artist.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :first_name %><br>
    <%= f.text_field :first_name %>
  </div>
  <div class="field">
    <%= f.label :last_name %><br>
    <%= f.text_field :last_name %>
  </div>
  <div class="field">
    <%= f.label :type %><br>
    <%= f.text_field :type %>
  </div>
  <div class="field">
    <%= f.label :phone_number %><br>
    <%= f.text_area :phone_number %>
  </div>
  <div class="field">
    <%= f.label :email_address %><br>
    <%= f.text_area :email_address %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
